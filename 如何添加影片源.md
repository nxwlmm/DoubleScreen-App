# 怎么把你自己的影片源配进去

> 一句话：**App 里什么都不带，源全部由你自己添加。**
> 添加方式有三种，最省事的是「批量粘贴」——一次能加几十条。

---

## 第 0 步 · 先确认手机里装的是新版

**这一步别跳过。** 早期构建出来的 APK 是**未签名**的，装的时候会报：

```
解析软件包时出现问题。(33)
packageInfo is null
```

如果你之前装的时候见过这个报错，说明当时那个包是坏包。请先按前面给的方法重新构建一次（`fixed_code_r9` 里的签名修复），拿到新包再继续。

**判断是不是新版**：新版安装过程中不会再出现上面那个报错，装完能正常打开。

---

## 方法一 · 批量粘贴（推荐）

适合：你手上有十几条、几十条链接，想一次全加进去。

### 操作步骤

**① 复制你的链接**

在浏览器、微信、备忘录里，把链接**整段选中复制**。格式随便，比如这样都行：

```
https://example.com/1.json      # 一号源
https://example.com/2.json      # 二号源
https://example.com/3.m3u
```

### ② 打开 App，点右上角的 `+`

或者点底部的 **「新增配置」**，两个是同一个入口。

### ③ 粘贴

长按输入框选「粘贴」，或者点下面的 **「从剪贴板粘贴」** 按钮。

### ④ 点「添加」

然后等几秒。App 会做两件事：**拆行解析** + **逐条测速校验**。

### ⑤ 看结果

- **成功**：卡片出现，状态是 **「可用」**，旁边显示延迟（如 `145ms`）
- **部分失败**：会提示"已添加 8 条，跳过重复 2 条"
- **某条显示「失效」**：说明这条链接当时连不上（正常现象，见最后的常见问题）

---

## 加进去之后，App 会自动做两件事

### 自动命名 —— 你不用起名

| 情况 | 卡片上显示的名字 |
|---|---|
| 你在行尾写了 `# 备注` | **用你的备注**（永远优先，不会被改） |
| 没写备注，但源里声明了名字 | **用源声明的名字**（如源里的 `sourceName`） |
| 没写备注，源里也没有名字 | **用域名**（如 `example.com`） |

也就是说：**直接粘链接就行，名称 App 自己会填。** 想自定义才需要写 `#`。

### 自动分类 —— 单仓 / 多仓 / 直播 自动判定

App 不是靠链接后缀猜的，而是**读内容判定**：

| 判定依据 | 分类 |
|---|---|
| JSON 里含 `storeHouse` 或 `urls`（子仓列表） | **多仓** |
| JSON 里是 `sites` 列表 | **单仓** |
| 内容含 `#EXTM3U` 或 `频道名,http://...` 两栏格式 | **直播** |

所以就算链接是 `.json` 结尾、内容其实是多仓，App 也会自动纠正成「多仓」——**你不需要在添加时选类型**。

> 添加弹窗里的「配置类型」三个按钮只在一种情况下有意义：链接既没有可辨识的后缀、内容又暂时探测不到。平时可以不管它。

---

## 方法二 · 单条添加

和上面一样，只是输入框里只粘一条。适合临时补一条。

---

## 方法三 · 电视端怎么加

电视上打字很痛苦，所以电视端**不用打字**，改用局域网推送：

1. 电视上：设置 → 源配置管理 → 焦点移到 **「新增配置」** → 按 OK
2. 电视屏幕会出现一个**二维码**，和一行网址（形如 `http://192.168.1.108:9978/?token=8a3f9e`）
3. 手机连**同一个 WiFi**，扫码或直接在浏览器打开那个网址
4. 手机上会看到一个输入框 → 粘贴你的链接 → 提交
5. 电视上自动接收并校验，卡片直接出现

> 电视端那个面板**不会常驻**——关掉面板就停止服务、链接作废。这是安全设计，避免同一网段的人乱推东西。

---

## 添加完之后怎么用

| 想做什么 | 怎么做 |
|---|---|
| **切换当前用的源** | 手机：点一下卡片（顶部会弹出「已切换到 XXX」+ **撤销**）；电视：焦点选卡片按 OK |
| **测速** | 手机：左滑卡片 → 「更新」；电视：MENU 键 → 检测更新 |
| **删除** | 手机：左滑卡片 → 「删除」；电视：MENU 键 → 删除 |
| **临时用一下不想留下记录** | 点顶部的 **「无痕」** 开关。开启后新加的源**只存在内存里，退出无痕就丢弃**，不写进手机存储 |

---

## 常见问题

**Q：加完了显示「失效」是怎么回事？**

可能的原因，按概率排：

1. **链接本身挂了** —— 这类源失效是常态。换一条即可。
2. **需要代理才能访问** —— 部分源部署在 GitHub、Pastebin 这类被墙的地址上。App 不会自动代理，你得让手机整体走代理。
3. **格式不被识别** —— App 支持的格式：单仓 JSON、多仓 JSON、M3U、TXT。其它格式会判为无效。
4. **服务器返回 404/403** —— 源已经被撤了。

想确认到底是哪一种：点一下卡片的「更新」重测一次；还是失效，基本就是源本身的问题。

**Q：为什么有的源点进去播不了？**

App 只负责把源配置读进来、列出来。**具体某个站点能不能播，取决于该站点自身的线路和播放器**，跟配置添加成功与否是两回事。

**Q：链接里有中文（比如 `http://某某.net/tv`）能识别吗？**

可以，直接粘。App 会自动把中文域名转成可解析的形式。

**Q：能一次粘贴几百条吗？**

能，但不建议。一次几十条比较合适——每条都要联网校验，几百条会等很久。

**Q：重复的链接会怎样？**

自动跳过，不会重复添加。提示里会告诉你跳过了几条。

**Q：添加的源存在哪？会不会丢？**

存在 App 私有目录的一个 JSON 文件里，卸载 App 才会丢。**开启了「无痕」时新增的源不会写进文件**，退出无痕即丢弃（这是无痕的定义）。

---

## 一句话总结

**复制链接 → 打开 App → 点 `+` → 粘贴 → 添加 → 点卡片设为当前。**

完事。
