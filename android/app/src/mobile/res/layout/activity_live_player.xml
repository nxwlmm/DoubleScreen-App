<?xml version="1.0" encoding="utf-8"?>
<!--
  直播播放页：全屏 PlayerView（自带控制器：进度/暂停/全屏切换），
  顶部保留一个返回 + 换台（上一个/下一个），方便纯手势操作。
  播放器实例由 LivePlayerActivity 持有，onDestroy 释放。
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FF000000">

    <androidx.media3.ui.PlayerView
        android:id="@+id/player_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:resize_mode="fit"
        app:surface_type="surface_view"
        app:use_controller="true" />

    <!-- 顶栏：返回 + 换台（播放器控制栏之外的最简操作） -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#66000000"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingTop="8dp"
        android:paddingEnd="16dp"
        android:paddingBottom="8dp">

        <TextView
            android:id="@+id/nav_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:fontFamily="monospace"
            android:padding="10dp"
            android:text="@string/nav_back"
            android:textColor="@color/text_main"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/now_playing"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/text_main"
            android:textSize="13sp"
            tools:text="CCTV-1 综合" />

        <TextView
            android:id="@+id/btn_prev"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:fontFamily="monospace"
            android:padding="10dp"
            android:text="@string/live_prev"
            android:textColor="@color/text_main"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/btn_next"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:fontFamily="monospace"
            android:padding="10dp"
            android:text="@string/live_next"
            android:textColor="@color/text_main"
            android:textSize="12sp" />
    </LinearLayout>

</FrameLayout>
