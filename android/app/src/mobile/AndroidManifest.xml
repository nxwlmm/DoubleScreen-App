<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <!--
          手机端入口。
          - windowSoftInputMode=adjustResize：添加配置弹窗里有输入框，
            不调整的话软键盘会盖住提交按钮
          - configChanges 拦截转屏：本页面是纯列表，重建一次反而会丢失左滑展开状态
        -->
        <activity
            android:name=".mobile.SourceConfigActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 直播：列表页 + 播放页。播放页锁定旋转由自身 configChanges 处理，
             避免横竖屏切换时 ExoPlayer 重建产生黑屏 -->
        <activity
            android:name=".mobile.LiveActivity"
            android:exported="false" />
        <activity
            android:name=".mobile.LivePlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:supportsPictureInPicture="true" />
    </application>

</manifest>
