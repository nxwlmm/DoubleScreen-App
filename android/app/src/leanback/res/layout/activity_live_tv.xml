<?xml version="1.0" encoding="utf-8"?>
<!--
  电视端直播频道列表页。
  与手机端同一个 LiveViewModel 驱动，差异只在交互方式：遥控器焦点导航。
  三段式结构与手机端一致（源 → 分组 → 频道），电视用户的学习成本最低。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_deep"
    android:orientation="vertical">

    <!-- 顶栏 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="40dp"
        android:paddingEnd="40dp">

        <TextView
            android:id="@+id/nav_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_input"
            android:focusable="true"
            android:fontFamily="monospace"
            android:padding="10dp"
            android:text="@string/nav_back"
            android:textColor="@color/text_dim"
            android:textSize="13sp" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_weight="1"
            android:text="@string/live_title"
            android:textColor="@color/text_main"
            android:textSize="20sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/btn_refresh"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_input"
            android:focusable="true"
            android:fontFamily="monospace"
            android:padding="10dp"
            android:text="@string/live_refresh"
            android:textColor="@color/accent_standard"
            android:textSize="13sp" />
    </LinearLayout>

    <!-- 直播源切换 -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="40dp"
        android:paddingTop="8dp"
        android:paddingBottom="4dp"
        android:text="@string/live_source_label"
        android:textColor="@color/text_dim"
        android:textSize="12sp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/source_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:orientation="horizontal"
        android:overScrollMode="never"
        android:paddingStart="36dp"
        android:paddingEnd="36dp"
        tools:itemCount="3"
        tools:listitem="@layout/item_live_source" />

    <!-- 分组切换 -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="40dp"
        android:paddingTop="14dp"
        android:paddingBottom="4dp"
        android:text="@string/live_group_label"
        android:textColor="@color/text_dim"
        android:textSize="12sp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/group_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:orientation="horizontal"
        android:overScrollMode="never"
        android:paddingStart="36dp"
        android:paddingEnd="36dp"
        tools:itemCount="5"
        tools:listitem="@layout/item_live_group" />

    <!-- 频道数 -->
    <TextView
        android:id="@+id/channel_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:paddingStart="40dp"
        android:paddingTop="12dp"
        android:paddingBottom="8dp"
        android:textColor="@color/text_dim"
        android:textSize="12sp"
        tools:text="共 128 个频道" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/channel_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:overScrollMode="never"
            android:paddingStart="36dp"
            android:paddingEnd="36dp"
            android:paddingBottom="32dp"
            android:scrollbars="vertical"
            tools:itemCount="8"
            tools:listitem="@layout/item_live_channel" />

        <LinearLayout
            android:id="@+id/loading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="vertical"
            android:visibility="gone">

            <ProgressBar
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_gravity="center" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="@string/live_loading"
                android:textColor="@color/text_dim"
                android:textSize="13sp" />
        </LinearLayout>

        <TextView
            android:id="@+id/error_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:lineSpacingExtra="5dp"
            android:padding="32dp"
            android:textColor="@color/text_dim"
            android:textSize="14sp"
            android:visibility="gone"
            tools:text="还没有可用的直播源" />
    </FrameLayout>

</LinearLayout>
