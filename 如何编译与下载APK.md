# 零基础：把代码传到 GitHub，并下载编译好的 APK

> 这份说明假设你**完全没接触过 Git 和 GitHub**。
> 全程只用浏览器，不需要装任何软件。
> 跟着做大约 10 分钟，之后每次改完代码，APK 会自动编译好等你下载。

---

## 第 1 步 · 把代码放进 GitHub（约 5 分钟）

### 1.1 注册 / 登录 GitHub

打开 <https://github.com> → 右上角 **Sign up**（没有账号）或 **Sign in**（已有账号）。

免费账号足够用，本项目的构建时长远低于免费额度。

### 1.2 新建一个仓库

1. 登录后点右上角 **`+`** → **New repository**
2. **Repository name** 随便填，比如 `myscreen`
3. 选 **Private**（私有，只有你能看）或 **Public**（公开）—— 都行，都能跑自动编译
4. **不要**勾选 "Add a README file"（我们已经有自己的文件了）
5. 点绿色按钮 **Create repository**

### 1.3 上传代码

创建完会看到一个空仓库页面，点中间的 **uploading an existing file** 链接
（或直接访问 `https://github.com/你的用户名/仓库名/upload/main`）。

然后把本项目文件夹里的**所有内容**拖进浏览器页面的虚线框：

```
📁 项目文件夹
├── 📁 .github          ← ⚠️ 这个文件夹必须一起传（自动编译的规则在里面）
├── 📁 android          ← 代码主体
├── 📁 docs
├── 📁 prototype
├── 📄 .gitignore
└── 📄 如何编译与下载APK.md
```

> **注意**：是拖**文件夹里面的东西**，不是把最外层文件夹本身拖进去。
> 传完后，仓库首页应该能直接看到 `android` 和 `.github` 两个文件夹。

最后点页面下方的绿色 **Commit changes**。

### 1.4 检查 `.github` 有没有传上去

这是最容易出问题的一步：`.github` 是**隐藏文件夹**，有些系统在拖拽时会跳过它。

回到仓库首页，确认能看到 **`.github`** 文件夹，点进去应该是：

```
.github/workflows/build.yml
```

**如果没有**，手动补一个：

1. 仓库首页 → **Add file** → **Create new file**
2. 文件名框里输入：`.github/workflows/build.yml`（斜杠会自动建文件夹）
3. 把我们提供的 `build.yml` 内容整个粘贴进去
4. 拉到底 → **Commit changes**

---

## 第 2 步 · 等它自动编译（约 5～10 分钟）

### 2.1 代码传上去的那一刻，编译就自动开始了

点仓库顶部菜单的 **Actions** 标签页。

你会看到一条正在跑的记录，名字叫 **Build APKs**，前面是黄色小圆点 🟡 表示正在编译。

### 2.2 看懂状态

| 图标 | 含义 | 你要做什么 |
|---|---|---|
| 🟡 黄色圆点 | 正在编译 | 等，期间可以关掉网页 |
| ✅ 绿色对勾 | **编译成功** | 去第 3 步下载 |
| ❌ 红色叉 | 编译失败 | 看下面的「编译失败了怎么办」 |

第一次编译会慢一些（要下载依赖，约 5～10 分钟）。
**之后每次改代码再传上来，只要 1～2 分钟。**

### 2.3 点进去能看到什么

点那条记录，会看到 4 个方块：

```
① 单元测试  →  ② 打包 · 电视版
            ↘  ③ 打包 · 手机版
            →  ④ 构建汇总
```

- ② 和 ③ 是**同时**跑的（并行），所以总时长不会翻倍
- 只有 ① 单元测试通过了，才会开始打包 —— 这是质量闸门

---

## 第 3 步 · 下载 APK（约 1 分钟）

### 3.1 进入构建详情页

**Actions** 标签 → 点进最新那条 ✅ 绿色的 **Build APKs**

### 3.2 找到下载区

把页面**拉到最底部**，有一个 **Artifacts** 区域，里面有两个可点的东西：

```
Artifacts
├── 📦 apk-leanback-tv      ← 电视版
└── 📦 apk-mobile-phone     ← 手机版
```

**点你要的那个**，浏览器会下载一个 `.zip` 文件。

### 3.3 解压

下载下来的 zip 里就是 APK 文件：

```
DoubleScreen-TV-a1b2c3d.apk      ← 电视版
DoubleScreen-Phone-a1b2c3d.apk   ← 手机版
```

> 名字后面那串 `a1b2c3d` 是这次提交的编号，用于区分不同版本，不影响安装。

---

## 装到设备上

### 手机

1. 把 APK 传到手机（微信文件传输助手 / QQ / 数据线都行）
2. 用手机的文件管理器点开这个 APK
3. 系统会提示"不允许安装未知来源应用" → 点**设置** → 允许该来源 → 返回继续安装

> 电视版和手机版**不要装错**。装错了也能用，但界面会很难受：
> 电视版是大字号焦点导航，手机版是触屏列表。

### 电视盒子

1. 把 APK 拷到 **U 盘**
2. U 盘插到电视/盒子上
3. 用设备自带的「文件管理」或「应用安装器」找到 U 盘里的 APK，点安装
4. 如果提示"未知来源"，在**设置 → 安全**里打开"允许安装未知应用"

> 部分盒子需要先在设置里开启 **ADB 调试**才能装第三方 APK，
> 具体路径因品牌而异（一般在 设置 → 关于 → 连点版本号 5 次）。

---

## 编译失败了怎么办（红叉 ❌）

### 先别慌，99% 是这三件事

**① 单元测试没过**

点开红色记录 → 左边点 **① 单元测试** → 展开失败的步骤，
找到形如 `FAILED` 或 `XXXTest > xxx FAILED` 的行。
这说明代码逻辑有问题，需要改代码。

**② `gradlew` 找不到**

报错里出现 `./gradlew: No such file or directory`。
不用管 —— 工作流里有一步会自动生成它。
如果这行报错出现在「准备 Gradle Wrapper」这一步本身，说明
`.github` 文件夹没传全，回第 1.4 步检查。

**③ 缺少 `gradle.properties`**

报错里出现 `This project uses AndroidX dependencies, but the 'android.useAndroidX' property is not enabled`。
说明 `android/gradle.properties` 这个文件没传上去，补传即可。

### 还是搞不定？

把红色报错页面**截图**发给技术人员（或直接问 AI），
关键是提供：**哪一步红的** + **红色的报错原文**。

---

## 常见问题

**Q：改了代码怎么重新编译？**

在 GitHub 网页上打开要改的文件 → 点右上角铅笔图标 ✏️ → 改完 → **Commit changes**。
一提交就会自动触发新的编译。

**Q：能只编译电视版吗？**

可以。Actions 标签 → 左边选 **Build APKs** → 右边 **Run workflow** 按钮 →
勾上「跳过单元测试」（如果想快）→ 绿色 **Run workflow**。
不过目前两个版本都会编译，因为它们的公共代码是同一份。

**Q：Artifacts 里的文件会一直留着吗？**

默认保存 **30 天**，过期自动清理。需要长期保留的话，
可以在 GitHub 上创建 **Release** 并把 APK 传进去（永久保存）。

**Q：Private 仓库也能用自动编译吗？**

能。GitHub Actions 对私有仓库的免费额度是每月 2000 分钟，
本项目一次完整构建约 6 分钟，够用 300 多次。

---

## 附：懂一点命令的话（可选，更快）

如果你愿意装 Git，可以用命令行上传，比网页拖拽可靠：

```bash
# 在项目文件夹里打开终端，依次执行
git init
git add .
git commit -m "初始化：双端源配置管理"
git branch -M main
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

之后每次改完代码只要三行：

```bash
git add .
git commit -m "说明这次改了什么"
git push
```

推上去之后，去 Actions 页面等绿灯就行。
