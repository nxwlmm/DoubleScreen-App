# 播放模块：方案评估与建议

> 结论先说：**"手机版 + 电视版的直播/电影/电视剧"这套完整功能，不是几轮对话能自研出来的。**
> 但有一条能自己走通的路 —— 见下方「建议」。

---

## 一、参考项目的真实规模

### FongMi/TV（社区里最主流的实现）

| 维度 | 实际情况 |
|---|---|
| 代码提交 | **6,728 次**（2022-06 至今，4 年持续开发） |
| 模块数 | app / catvod / quickjs / chaquo / forcetech / hook / jianpian / thunder / tvbus / zlive / buildSrc / website |
| 播放引擎 | **双引擎**：Media3(ExoPlayer) + mpv，硬解 + FFmpeg 软解自动降级 |
| 爬虫引擎 | **三套**：JavaScript(QuickJS) / Python(Chaquopy) / Java JAR(DexClassLoader) |
| 构建要求 | **JDK 21 + Python 3.10 + Android SDK** |
| 直播能力 | M3U / TXT / JSON 三种格式 + XMLTV 节目单 + 时移回看 |
| 其他 | 弹幕、字幕(SRT/SSA/ASS)、倍速、画中画、DLNA 投屏、Android Auto、本地 HTTP 控制 API |

### takagen99/Box（社区"黑版"，fork 自猫影视 TVBox）

在原版基础上重构了 UI 渲染层与插件加载机制：多 Jar 并行加载（多 DexClassLoader 实例避免类冲突）、多源异步并发检索、阿里云盘直连。

---

## 二、一个值得注意的印证

FongMi/TV 的 README 原文：

> **「App 本身不內建或提供任何內容來源。」** 外部內容需自行配置。

**这和我们一直坚持的立场完全一致** —— 社区里最成熟、最主流的实现，也是不内置任何源、由用户自行配置。这不是我们保守，是这个领域的通行做法。

---

## 三、真正的技术门槛：Spider 引擎

这是决定"能不能自己从头做"的关键。

### 直播源 —— **能自己做** ✅

```
M3U / TXT 直播源的内容长这样：
    #EXTM3U
    #EXTINF:-1 group-title="央视",CCTV-1
    http://xxx/cctv1.m3u8
                    ↑ 频道名 + 播放直链，直接就能喂给播放器
```

**不需要任何插件**，解析出来就能播。用 ExoPlayer 即可。

### 点播源（JSON 单仓/多仓）—— **做不了** ❌

```
JSON 源里长这样：
    { "sites": [ { "key": "douban", "name": "豆瓣", "type": 3, "api": "csp_Douban" } ] }
                                                                  ↑ 这个
```

`api` 指向的不是网址，而是**一个 Spider 插件里的类名**。要拿到播放地址，必须：

1. 动态加载并运行第三方 Java/JS/Python 代码（三套引擎）
2. 实现完整的 Spider 生命周期（`homeContent` / `detailContent` / `playerContent` / `searchContent` / `proxy`）
3. 内置 JS 运行时（含 HTTP、DOM 解析、加密库）
4. 处理各站点千奇百怪的返回值

**这是 FongMi 四年积累的核心资产**，不是补几个文件能追上的。

---

## 四、还有一个硬障碍

FongMi 的 README 明确写着：

> `lib-*.aar` 未納入 Git，**單純 clone 不包含完整播放器依賴**。

也就是说：**直接 clone 它的仓库根本构建不起来**，还缺播放器的预编译依赖。这意味着"基于它二次开发"这条路，第一步就会卡在依赖获取上。

另外它的构建要求 **JDK 21 + Python 3.10**，而我们当前工程是 JDK 17。

---

## 五、三条路线对比

| | A. 只做直播播放 | B. 基于 FongMi 二次开发 | C. 从零自研完整播放器 |
|---|---|---|---|
| **能看直播** | ✅ | ✅ | ✅ |
| **能看电影/剧** | ❌ | ✅ | ✅ |
| **工作量** | 中等（几轮） | ⚠️ 需先解决 AAR 依赖，且要读透 6700 次提交的代码 | ❌ 4 年 × 团队 |
| **可控性** | 全部自己掌握 | 受上游影响 | 全部自己掌握 |
| **可行性** | ✅ **确定可做** | ⚠️ 有未知障碍 | ❌ 不现实 |
| **能保住现有成果** | ✅ 完全复用 | ⚠️ 现有源管理模块可能被替换掉 | ✅ 完全复用 |

---

## 六、建议

### 分两步走

**第一步：做直播播放（推荐先做）**

- 你清单里的直播源，**本来就是频道名 + 直链**，不需要 Spider
- 能实现：直播源选择 → 频道列表（按 `#genre#` 分组折叠，如"央视/卫视/港澳台"）→ 点击播放
- 手机端 + 电视端都做（电视端遥控器操作）
- **这是你能"真的看上电视"的最短路径**

**第二步：点播怎么办**

诚实讲，只有三个选择：

1. **看电影/剧用 FongMi 或 Box 的现成 APK**，我们的 App 专注做"源管理 + 直播"（各司其职）
2. **等第一步做完再评估** Spider 引擎 —— 我建议慎重，那是另一个量级
3. 如果你能拿到 `lib-*.aar`，可以在 FongMi 基础上改一套自己的皮肤/包名/图标，但那是"改别人的 App"，不是"我们的 App 长出了播放能力"

### 不推荐

**不要试图从零实现 Spider 引擎。** 那需要同时维护 JS 运行时、Java 动态加载、Python 解释器三套引擎，还要跟上游各站点的反爬变化 —— 这是长期维护成本，不是一次性开发任务。

---

## 七、待你决定

1. **先做直播播放吗？**（我建议做，且能确定交付）
2. 电影/剧这块，接受"用 FongMi 的现成 APK"吗？
3. 如果坚持要自己的 App 也能点播 —— 需要先解决 `lib-*.aar` 的来源问题，这个我这边无法凭空解决，需要你确认能否从上游拿到。
